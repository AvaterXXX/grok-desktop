# 更新日志

安装包：https://github.com/AvaterXXX/grok-desktop/releases

本仓库发布 **Windows 便携版和安装版 exe**。Linux / macOS 请从源码构建（见 README）。

---

## [0.1.17] - 2026-09-08

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 多图显示 | 粘贴多张截图发送后只显示 1 张：粘贴图片统一叫 paste.png 被误判为重复；改为按内容判重，模型收到的和气泡显示的一致；不同目录同名文件不再互相顶掉 |
| 草稿串台 | 切换会话后输入框残留上一会话未发送的文字，并被误存为所经会话的"幽灵草稿"长期复活；现在输入框按会话各自保存/恢复草稿，新建会话输入框清空 |
| 停止锁死 | 快速切换会话会让加载标志泄漏，任务运行中输入框和停止按钮被禁用、而右下角计时器仍在走；补上泄漏复位，并保证任务运行中停止/排队永远可点 |
| 模型版本名 | 回复名称标签显示带版本的模型名（如 Grok 4.6），按各会话自己的模型显示，历史回放一致 |
| 命令与模式徽章 | 斜杠命令选中后输入框上方显示紫色命令徽章（含说明、可一键移除）；激活模式（plan）蓝色、目标模式（goal）琥珀色徽章 + 输入区着色，三态一目了然 |
| 文件变更卡 | "本轮改了 N 个文件"列表超过约 8 行出现内部滚动条，不再撑爆消息流；对话进行中不再提前弹出，等本轮结束才展示 |

下载：`Grok-Desktop-0.1.17-Windows-Portable-x64.exe`

---

## [0.1.16] - 2026-09-06

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 侧边栏排序 | 新建会话不再沉底：未拖拽过的会话按更新时间浮在分组顶部，拖拽过的保持手动位置；拖拽落点与所见位置一致 |
| 文件路径链接 | 消息正文、代码引用与代码块标题中的本地路径变成可点击链接，点击用默认程序打开；右键可选「打开 / 在文件夹中显示 / 复制完整路径」 |
| 路径解析 | 相对路径按会话工作目录补全；Windows 绝对路径直接识别；版本号（如 0.1.15）和纯文件名不会被误当成路径 |
| 后台标签流式 | 后台标签的窗格未挂载到文档导致存活判断失效、回复碎成单 token 气泡；改为按会话窗格树判断，后台会话流式完整 |
| 思考/正文交错 | 思考与正文 token 交错到达时，正文不再反复开关思考折叠区，消除一个 token 一个 0.1 秒思考块的现象 |
| 步骤归属 | 正文已流出后到达的工具、权限请求与文件差异落到顶层，不再嵌进上方思考区，时间线顺序保持 |
| 历史思考块 | 重放的思考块仅在仍是窗格最新节点时延续合并，中途思考不再被提升到它所属的消息上方 |
| 长回复分页 | 最新用户问题的扫描窗口扩大到 3 页且不少于 240 行，工具密集的回复不再把问题挤出首屏 |
| 回归测试 | 覆盖路径识别与链接化、流式边界策略、后台窗格存活判断和侧栏排序 |

下载：`Grok-Desktop-0.1.16-Windows-Portable-x64.exe`

---

## [0.1.15] - 2026-09-04

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 思考原文 | 不再根据末尾标点猜测“片段中断”，不删除空编号、不追加省略号，实时与历史都按原始事件展示 |
| 思考内步骤 | 思考阶段触发的工具、权限请求与文件差异归入对应思考折叠区；标题显示其中步骤数 |
| 文件聚合 | 同一思考阶段按规范化完整路径合并同文件的多次修改并累计差异；同名但不同目录的文件不会误合并 |
| 路径展示 | 文件卡直接显示 `deploy/README.md` 一类相对路径，避免多个同名文件无法区分 |
| 压缩历史 | 会话压缩后从追加式 ACP 更新日志恢复压缩前时间线，当前长会话不再只剩最近一个问题 |
| 自动翻页 | 移除顶部“更早的 N 条”按钮，滚动到顶部自动加载上一批并保持阅读位置 |
| 目标状态 | 接入真实 `goal_updated` 生命周期；目标模式和右上角目标面板会显示当前目标，完成或暂停状态不会在重启后误发“继续目标” |
| 目标清除 | `cleared`、完成和取消统一视为终止态；清除后写入终止标记，旧会话也会识别 `/goal clear`，重启不再恢复或提示不存在的目标 |
| 消息恢复 | 用户消息先原子保存再交给助手；即使发送后立即退出，重开也能恢复尚未写入官方历史的消息 |
| 消息时间 | 用户和助手消息标题旁显示发送时间，历史记录优先读取 ACP 事件时间 |
| 连续思考 | 同一回答阶段只用一个外层思考块，内部严格按“思考 → 工具 → 思考 → 工具”时间线交错展示，不再把正文和工具分别堆在上下两处 |
| 流式正文归并 | 过程说明流式更新始终复用当前消息；只有用户消息、工具/权限/文件步骤和本轮结束才切换，避免每个 token 生成独立气泡 |
| 重启历史顺序 | 恢复尚未写入标准历史的用户消息时按发送时间插回原位置；目标/计划模式的隐藏命令前缀也能正确匹配，不再把旧问题追加到最终答案之后 |
| 会话恢复写入 | 草稿或消息变化只向主进程发送本次补丁，并在 IPC 前限制恢复文本体积，避免长会话反复出现 `session sidecar is too large` |
| 代码清理 | 统一三处日志尾部读取并保证文件句柄关闭，删除退役的目标卡样式和重复恢复调用 |
| 回归测试 | 覆盖原文保真、目标完成迁移、消息时间、发送恢复、思考与工具归属、压缩历史和自动分页 |

下载：`Grok-Desktop-0.1.15-Windows-Portable-x64.exe`

---

## [0.1.13] - 2026-09-01

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 流式思考 | 同一连续思考内的 token 追加到一块；过程说明、工具或新用户消息之后的思考另起一块并保留原位置 |
| 过程说明 | 连续正文始终留在同一条消息，不再每几个字生成一张气泡 |
| 空白 token | 不生成空思考或空消息，也不再暗中截断前后文本 |
| 最后一行 | 一轮结束前同步刷新剩余缓冲，避免思考或回答最后一行缺失 |
| 历史顺序 | 删除无事件位置的整轮思考/回答恢复和二次 DOM 合并，重开会话不再把内容猜测性地挪到顶部 |
| 代码清理 | 合并重复的流缓冲刷新代码，移除失效的流阶段、历史合并和大字符串恢复状态 |

下载：`Grok-Desktop-0.1.13-Windows-Portable-x64.exe`

---

## [0.1.12] - 2026-08-31

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 思考顺序 | 按事件分段，工具后的思考留在工具下方；历史恢复不再把整轮思考堆到顶部 |
| 工具步骤 | 完成的多步骤默认折叠；运行或失败的步骤保持展开 |
| 文件改动 | 编辑状态与差异卡合并；成功时不再重复显示完成卡，失败直接标在文件标题 |
| 历史与搜索 | 会话索引缓存、异步搜索与过期请求取消；历史图片按需加载 |
| 稳定性 | 引入会话状态 reducer、原子批量持久化、版本迁移和 ACP 恢复静音 |
| 安全 | 启用 Electron 沙箱，并校验 IPC 参数、调用来源、外链和路径边界 |
| 展示与无障碍 | 修复加载提示、顶部历史入口、键盘焦点、窄窗口及 125%/150% DPI 显示 |
| 工程质量 | 增加 32 个单元测试，以及 Lint、格式、类型、CSS、性能和跨平台 CI 检查 |
| 运行时 | 升级至 Electron 44；依赖审计 0 漏洞 |

下载：`Grok-Desktop-0.1.12-Windows-Portable-x64.exe`

---

## [0.1.11] - 2026-08-26

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 说完卡住 | 一轮结束不再把整段思考渲成 markdown，窗口不会因此卡死 |
| 思考钟 | 搜索、读文件不会提前把钟关掉 |
| 思考位置 | 只跟在本轮用户消息后面，不再写进上一轮，也不会顶到最上面盖住旧对话 |

下载：`Grok-Desktop-0.1.11-Windows-Portable-x64.exe`

---

## [0.1.10] - 2026-08-22

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 思考 | 一轮一块；搜索、读文件不会拆成新块，也不会把钟关掉 |
| 思考和回复 | 各写各的，不合进同一条气泡 |
| 本轮改文件 | 按文件合并；点开看这一轮各次改动，不跳到中间卡片 |
| 周限额 | 按刷新时刻重算，不再等于当日 |
| 启动 | 打开上次关掉的对话，没有再打开最新 |
| 暂停 | 重开不再续跑；输入和进行中的思考、回复能回来；长回复不截断 |
| 思考面板 | 加宽约 20%，历史可点开看全文 |

下载：`Grok-Desktop-0.1.10-Windows-Portable-x64.exe`

---

## [0.1.9] - 2026-08-20

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 超时 | 一轮太久会提示，不再干等到 ACP 断开 |
| 侧栏 | 可拖宽；对话、记忆、技能、插件默认一行，窄了再折 |
| 气泡 | 用户和助手对比加大 |
| 插队 | 发出你写的正文，不再改成「继续目标」 |
| 用户图片 | 跟在用户气泡里，不再落到助手后面 |
| CLI 退出 | 进程掉了会重连；已出字的请再发一次 |
| 钉条 | 点了跳到原文并展开；滑走再钉回来；再往上滑换成上一条 |
| 改动卡 | 删行浅红、增行浅绿；大段删除先收起来露出新增 |
| 目标 | 清单勾完立刻清掉；进对话不自动打开目标 |

下载：`Grok-Desktop-0.1.9-Windows-Portable-x64.exe`

---

## [0.1.8] - 2026-08-20

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 排队 | 按会话分开；当前条跑完自动发下一条；空闲自动发出，不用点插队 |
| 删排队 | 删一条不再带走另一条；新对话看不到别的队列 |
| 切会话 | 滚到该会话最底 |
| 加粗 | 先抽出行内代码再加粗，避免代码里的 `*` 把 `**` 打出来 |
| 思考计时 | 按会话、按当前这一步分开计，不整轮累加，也不串到别的会话 |

下载：`Grok-Desktop-0.1.8-Windows-Portable-x64.exe`

---

## [0.1.7] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 思考档 | 按当前模型列出，不写死 Low/Medium/High/xhigh |
| CMD | 左下角在当前工作区开终端；设置开了代理才带 HTTP_PROXY 和 HTTPS_PROXY |
| 工程/改文件 | 认官方 notify 和 title，审核 diff 出卡 |
| 发送错误 | JSON-RPC 错误展开，不再显示 [object Object] |
| /call | 调用方留下等待线程，刷对方思考/读改文件，干完交回 |
| 侧栏 | 目录加底和竖线，对话缩进 |
| 思考块 | 能折叠，箭头加大 |
| 用户气泡 | 名字在图片和文字上面 |

下载：`Grok-Desktop-0.1.7-Windows-Portable-x64.exe`

---

## [0.1.6] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 子代理面板 | 挪到右上角，点开常驻，点别处不收，再点收回 |
| 和目标叠放 | 两个都开时在对话右侧上下堆叠，不再盖住顶栏 |
| 工具卡箭头 | 右侧展开箭头再加大 |
| 压缩状态 | 只有真 `/compact` 才显示，不再锁死「正在压缩上下文」 |

下载：`Grok-Desktop-0.1.6-Windows-Portable-x64.exe`

---

## [0.1.5] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 周限额 | 启动立刻画缓存额度；空数据不再把数字刷成 — |
| 上下文环 | 按真实占用涨，不再卡在第一次估算 |
| 子代理 | 只认官方 spawn / finish，grep、权限、Kill task 不再开卡 |
| 目标续跑 | 清单全勾完不再自动「继续目标」；点暂停 / 停止也不会再发一次 |
| 侧栏 | 对话和工程可拖拽排序，运行中的置顶 |
| 压缩 | 跑 compact 时显示「正在压缩上下文」 |
| 工具卡箭头 | 更大、好点；收起朝右，展开朝下 |
| 排队编辑 | 不再出现 Windows 黑框按钮 |

下载：`Grok-Desktop-0.1.5-Windows-Portable-x64.exe`

---

## [0.1.4] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 会话 ID | 正文里的 UUID 原样发出；只有单独贴 ID，或 `/call <id> 消息`，才切会话 |
| 排队可编辑 | 点文字或「编辑」，回车保存，Esc 取消；生成中一打字变发送 |
| 子代理 | 输入框上方各占一行；点开看一整段工作记录；主任务结束不再卡在进行中 |
| 斜杠菜单 | 按第一段过滤，空格后收起；未入表命令不再显示无匹配 |
| 目标 / 计划 | 输入框上方纸卡片：目标全文、计划步骤；不要紫条 |
| 侧栏 | 项目 / 对话字号和行距加大 |
| 顶栏用量 | 周限额和周 Token 留下，后面加真·当日（上海时区） |
| 个人信息 | 点设置默认进这一页；居中、可读宽度；今年 1–12 月 Token 热力图 |
| 助手气泡 | 不再贴复制 / 分叉 / 记记忆 |

源码：tag `v0.1.4`。本版未重打 Windows 便携 exe。

---

## [0.1.3] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 当日 API 标价 | 当日 Token 后显示估算金额，随用量更新 |
| 悬停拆 4.5 / 4.6 | 鼠标放在金额上可看 Grok 4.5 与 Grok 4.6 各花多少（缓存价不同） |
| 每轮用量 | 助手气泡按本轮实际模型展示入 / 出 / 缓存和估算 |
| 含未发布的 0.1.2 桌面修复 | 默认 4.6 + Extra High、主题对比度、壁纸、加载更早、用户图片、滚动跟随、重开看全最后一轮 |

下载：`Grok-Desktop-0.1.3-Windows-Portable-x64.exe`

---

## [0.1.2] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 启动 / 新对话 / 恢复会话 | 默认 Grok 4.6 + Extra High（xhigh），不再停在 High |
| 深色主题对比度 | 主按钮、周限额条、顶栏图标、命令完成卡跟暗色 token |
| 浅色聊天背景 | 保存后对话区能看见壁纸，不再被不透明底盖住 |
| 加载更早消息 | 停在刚加载的一段，不再跳回最新 |
| 用户发送的图片 | 跟在用户消息下，不贴到助手回复末尾 |
| 滚动跟随 | 在底部才跟随输出；上翻出现回到最新箭头 |
| 重开会话 | 看全最后一轮（含长回复），滚到真正底部 |

下载：`Grok-Desktop-0.1.2-Windows-Portable-x64.exe`

---

## [0.1.1] - 2026-08-19

隐私：不收集、不上传、无遥测。登录只走本机官方 grok CLI。

| 项 | 说明 |
|------|------|
| 新对话默认任务模式 | 不再残留 /goal |
| 最后一条用户消息 | 可编辑 / 撤回 |
| /effort /model /usage | 斜杠走本地，不当聊天任务 |
| 生成中可排队 | Codex 细条；插队 / 删除 |
| 删会话只清本地 | 不卡 CLI |
| 重启恢复 | 思考/工具卡/目标条 |
| 贴图立刻可见 | 粘贴图片马上显示 |
| Diff / 计划条跟主题 | 转圈颜色跟系统主题 |
| 同一轮不重复展示 Grok | 设置可改昵称/头像 |
| 代理回填 | 空保存不覆盖 |
| 长气泡可滚 | 流式不再三重滚动 |

下载：`Grok-Desktop-0.1.1-Windows-Portable-x64.exe`

---

## [0.1.0]

社区叉基线。桌面壳套在官方 grok CLI 上。

喜欢就点一颗 Star：https://github.com/AvaterXXX/grok-desktop
